# Shelfly.Common — Agent Notes

- Shared domain models (Book, Bookmark) referenced by both Api and App.
- Persistence entity models live in `Shelfly.Api/Data/Entities/`, separate from these domain classes.
