<?xml version="1.0" encoding="utf-8"?>

<pages:ShelflyContentPageBase
    x:Class="Shelfly.App.Features.Trash.Pages.TrashBookmarkDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="using:Shelfly.App.Resources.Localization"
    xmlns:viewModels="clr-namespace:Shelfly.App.Features.Trash.ViewModels"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:pages="clr-namespace:Shelfly.App.Pages"
    x:DataType="viewModels:TrashBookmarkDetailViewModel"
    Title="{x:Static local:AppResources.TrashBookmarkDetailPageTitle}">

    <Grid RowDefinitions="*" ColumnDefinitions="*">
        <!-- Note Content -->
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Spacing="16" Padding="16">
                <ActivityIndicator IsRunning="{Binding IsLoading}"
                                   IsVisible="{Binding IsLoading}"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"
                                   HeightRequest="40"
                                   WidthRequest="40" />

                <Label Text="{Binding Note}"
                       FontSize="16"
                       LineBreakMode="WordWrap"
                       VerticalOptions="Start"
                       IsVisible="{Binding Note, Converter={toolkit:IsStringNotNullOrWhiteSpaceConverter}}" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</pages:ShelflyContentPageBase>
